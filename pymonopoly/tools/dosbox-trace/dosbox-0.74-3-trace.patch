--- a/include/paging.h	2019-06-18 15:39:08.000000000 +0000
+++ b/include/paging.h	2026-08-17 01:25:31.591536502 +0000
@@ -24,6 +24,7 @@
 #ifndef DOSBOX_DOSBOX_H
 #include "dosbox.h"
 #endif
+#include "monolog.h"
 #ifndef DOSBOX_MEM_H
 #include "mem.h"
 #endif
@@ -290,6 +291,10 @@
 }
 
 static INLINE void mem_writew_inline(PhysPt address,Bit16u val) {
+	/* The CPU core stores through this, not through mem_writew(), so a
+	 * watch on the public helper never fires.  This is the real path. */
+	if (mono_watch() && address>=mono_watch() && address<mono_watch()+mono_watchlen())
+		mono_log("W16 %06X = %04X", (unsigned)address, (unsigned)val);
 	if ((address & 0xfff)<0xfff) {
 		HostPt tlb_addr=get_tlb_write(address);
 		if (tlb_addr) host_writew(tlb_addr+address,val);
--- a/src/ints/bios_keyboard.cpp	2019-05-02 17:01:15.000000000 +0000
+++ b/src/ints/bios_keyboard.cpp	2026-08-17 01:29:51.625883374 +0000
@@ -19,6 +19,7 @@
 /* $Id: bios_keyboard.cpp,v 1.36 2009-06-11 16:05:17 c2woody Exp $ */
 
 #include "dosbox.h"
+#include "monolog.h"
 #include "callback.h"
 #include "mem.h"
 #include "bios.h"
@@ -491,6 +492,37 @@
 
 static Bitu INT16_Handler(void) {
 	Bit16u temp=0;
+	/* the int pushed IP, CS, FLAGS -- so SS:SP names the caller */
+	if (mono_log_int()) mono_log("INT16 AH=%02X from %04X:%04X", (unsigned)reg_ah,
+	         (unsigned)mem_readw(SegPhys(ss)+reg_sp+2),
+	         (unsigned)mem_readw(SegPhys(ss)+reg_sp));
+	if (mono_keys_len()) {
+		/* Canned input: a key becomes available every mono_rate()
+		 * polls, so the pacing is a count rather than a clock and two
+		 * runs see the same keys at the same points in the program. */
+		static unsigned mono_poll = 0;
+		static int mono_idx = 0;
+		if (mono_stop() && mono_idx >= mono_stop()) {
+			if (reg_ah==0x01 || reg_ah==0x11) { CALLBACK_SZF(true); return CBRET_NONE; }
+		}
+		if (reg_ah==0x01 || reg_ah==0x11) {
+			mono_poll++;
+			if (mono_poll % mono_rate()) {
+				CALLBACK_SZF(true);          /* nothing waiting */
+				return CBRET_NONE;
+			}
+			reg_ax = (0x1c<<8) | mono_key_at(mono_idx);
+			CALLBACK_SZF(false);
+			return CBRET_NONE;
+		}
+		if (reg_ah==0x00 || reg_ah==0x10) {
+			reg_ax = (0x1c<<8) | mono_key_at(mono_idx);
+			mono_log("  -> feeding key %02X (#%d)",
+			         (unsigned)mono_key_at(mono_idx), mono_idx);
+			mono_idx++;
+			return CBRET_NONE;
+		}
+	}
 	switch (reg_ah) {
 	case 0x00: /* GET KEYSTROKE */
 		if ((get_key(temp)) && (!IsEnhancedKey(temp))) {
--- a/src/dos/dos.cpp	2019-05-02 17:01:19.000000000 +0000
+++ b/src/dos/dos.cpp	2026-08-17 01:21:53.396268736 +0000
@@ -23,6 +23,7 @@
 #include <ctype.h>
 #include <time.h>
 #include "dosbox.h"
+#include "monolog.h"
 #include "bios.h"
 #include "mem.h"
 #include "callback.h"
@@ -79,6 +80,11 @@
 
 #define DOSNAMEBUF 256
 static Bitu DOS_21Handler(void) {
+	if (reg_ah==0x01||reg_ah==0x06||reg_ah==0x07||reg_ah==0x08||
+	    reg_ah==0x0a||reg_ah==0x0b||reg_ah==0x0c)
+		if (mono_log_int()) mono_log("INT21 AH=%02X from %04X:%04X", (unsigned)reg_ah,
+		         (unsigned)mem_readw(SegPhys(ss)+reg_sp+2),
+		         (unsigned)mem_readw(SegPhys(ss)+reg_sp));
 	if (((reg_ah != 0x50) && (reg_ah != 0x51) && (reg_ah != 0x62) && (reg_ah != 0x64)) && (reg_ah<0x6c)) {
 		DOS_PSP psp(dos.psp());
 		psp.SetStack(RealMake(SegValue(ss),reg_sp-18));
--- a/src/hardware/memory.cpp	2019-05-02 17:01:18.000000000 +0000
+++ b/src/hardware/memory.cpp	2026-08-17 01:02:53.594584514 +0000
@@ -19,6 +19,8 @@
 /* $Id: memory.cpp,v 1.56 2009-05-27 09:15:41 qbix79 Exp $ */
 
 #include "dosbox.h"
+#include "monolog.h"
+#include "regs.h"
 #include "mem.h"
 #include "inout.h"
 #include "setup.h"
@@ -500,10 +502,18 @@
 }
 
 void mem_writeb(PhysPt address,Bit8u val) {
+	if (mono_watch() && address>=mono_watch() && address<mono_watch()+4)
+		mono_log("WRITE8  %06X = %02X  from %04X:%04X",
+		         (unsigned)address,(unsigned)val,
+		         (unsigned)SegValue(cs),(unsigned)reg_ip);
 	mem_writeb_inline(address,val);
 }
 
 void mem_writew(PhysPt address,Bit16u val) {
+	if (mono_watch() && address>=mono_watch() && address<mono_watch()+4)
+		mono_log("WRITE16 %06X = %04X  from %04X:%04X",
+		         (unsigned)address,(unsigned)val,
+		         (unsigned)SegValue(cs),(unsigned)reg_ip);
 	mem_writew_inline(address,val);
 }
 
